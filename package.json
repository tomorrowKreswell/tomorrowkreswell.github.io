{
  "name": "resume",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://tomorrowkreswell.github.io/tomorrowkreswell.github.io",
  "dependencies": {
    "react": "^16.6.0",
    "react-dom": "^16.6.0",
    "react-scripts": "2.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy:script": "gh-pages -d build",
    "predeploy": "npm run build",
    "deploy": "NODE_OPTIONS=--openssl-legacy-provider npm run deploy:script"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "dev": "set NODE_OPTIONS=--openssl-legacy-provider && vue-cli-service serve",
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
